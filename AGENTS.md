# 项目协作规范

本文件规定 Agent 在本仓库中的协作行为。产品、技术、数据和实施细节以 `DOCUMENT_MAP.md` 映射的项目文档为准，本文件不重复抄写。

## 文档索引与优先级

- 文档地图：[`DOCUMENT_MAP.md`](DOCUMENT_MAP.md)。开始任务前先读取它，按其中的实际路径定位需求、决策、进度和设计文档。它只负责导航，不记录产品或技术事实，也不改变本文件定义的优先级。
- 优先级：本文件 → 用户本次明确确认的需求或方案 → `DOCUMENT_MAP.md` 指向的需求/决策/产品/技术文档 → 进度文档 → 相关代码、测试、配置与脚本。
- 冲突时采用更高优先级规则；代码与文档不一致或存在会影响范围的未知事项时，记录差异并向用户确认，不能静默猜测。

## 项目概况

「考研备考工作台」是一个个人使用的纯前端响应式 Web 应用（无后端、无数据库、无框架）。核心价值是**错题归类（四栏归因法）+ 上传图片记录错题/知识图片**。用户是唯一使用者，近期目标是 12 月研究生考试。

## 首次接入

1. 本仓库为首次接入，无既有 AGENTS / DOCUMENT_MAP，使用本技能建立治理结构。
2. 已有 `docs/specs/` 存放 PRD 与技术方案，沿用该目录，不另建平行 docs。
3. 新建 `Requirements/`、`Progress/`（加上 `Decisions/` 视需要）承载台账与进度。

## 每次任务的确认流程

1. 阅读相关需求、决策、进度、产品、技术、UI、业务流程和受影响代码/测试，先理解当前业务主流程、数据和边界。
2. 分类请求：Bug 进入 Bug 台账；功能、功能优化和体验优化进入需求台账。涉及多模块、图片存储、事件联动或跨页传播时按跨模块处理并建立详细需求记录。
3. 向用户确认目标、范围、不做项、验收标准、数据影响和未决问题；描述完整且风险很低时，将确认口径写入记录后继续。会改变范围、数据、兼容性或成本的未知事项，必须获得用户明确答复。
4. 在确认口径内提出最小完整计划，写清改动层、依赖、风险、验证和文档联动。存在多种合理方案或难以回退影响时，先列出方案、推荐项和理由，待确认后实现。

## 数据与存储变更

新增持久化数据、变更数据结构（localStorage/IndexedDB 的键、实体、字段、约束、关系）、迁移历史数据或删除数据前，必须先出数据设计方案并和用户确认。方案包含现状与依赖、可选方案、推荐理由、实体/字段/约束、待确认 schema、迁移/备份与回滚、数据生命周期。

未经用户确认，不得把待选方案实现为 UI/存储行为。实施中若数据语义或影响范围变化，暂停并重新确认。

## 计划与进度

这是一个跨多个功能模块、多阶段实现的大任务，必须创建功能进度文档（`Progress/PROG-*`），并将其路径写入需求台账和功能进度台账；每个阶段开始、完成、阻塞时更新状态、证据、风险和下一步。需求最终完成后保留验证证据和遗留项。

## 实现、验证与 Git

- 技术栈为纯前端（HTML/CSS/JS），无框架、无构建，直接浏览器打开。遵循 `docs/specs/TECH-*.md` 的架构与代码组织约定。
- 只修改当前任务范围，保留无关用户改动；不顺手重构，不为未确认需求预建复杂能力。
- 文档、决策和进度与实现同步更新。相关文档无需更新时，在需求或交付记录中写明理由。
- 按风险运行相关检查（浏览器实际打开验证响应式、数据持久化、错题全链路、图片链路），如实报告结果；跨模块需求逐项回填行为验收矩阵。
- 提交前检查工作区、分支、远程与忽略规则，只暂存本次工作。未经用户明确授权，不推送、发布、部署或创建 PR。
- 不提交密钥、`.env*`、本地图片缓存、备份、日志和构建产物，除非项目明确要求。