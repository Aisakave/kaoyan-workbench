# Bug 台账

## 状态约定

- 待确认 / 复现中 / 修复中 / 待验证 / 已关闭 / 无法复现 / 不修复（保留原因）

| ID | 日期 | 严重度 | 标题 | 状态 | 影响范围 | 关联流程/测试 | 可复现或可验证的用户行为 | 摘要/备注 |
|----|------|--------|------|------|----------|---------------|--------------------------|----------|
| BUG-20260915-001 | 2026-09-15 | P1 | 导出备份时图片数据丢失（Blob JSON 序列化后为空对象 `{}`） | 已关闭 | 所有含图片的记录（错题簿图片、资料库图片）导出→导入恢复失效 | localStorage/IndexedDB 图片链路；导出/导入 | 用户导出备份，JSON 中 `images` 数组的图片 value 为 `{}`；导入后图片丢失 | 根因：`Blob` 不能直接 `JSON.stringify`，序列化后变空对象。修复：导出时 FileReader 转 base64 dataURL，导入时 atob 还原 Blob 写 IndexedDB；兼容旧版空对象备份（跳过+提示）。已改 `js/controller.js`。验证：Node 字节级往返测试（257B PNG dataURL 还原 size/字节一致），浏览器控制台确认图片入 IndexedDB 正常、页面无报错。 |
| BUG-20260915-002 | 2026-09-15 | P2 | 复习弹窗右上角 ✕ 关闭按钮失效（点 X 无反应，被强制二选一） | 已关闭 | 错题簿复习弹窗 | `wrongBook.js` 复习弹窗；复习流程 | 打开复习弹窗后点 ✕ 无反应，只能点「做对了/又做错了」退出 | 根因：`openReview` 用 `modalShell` 渲染带 ✕ 标题栏但漏调 `bindModalEvents()`，✕ 未绑定关闭事件（其余 6 视图弹窗均调用）。修复：补调 `bindModalEvents()`。验证：浏览器实测 ✕ 点击后弹窗关闭、做对流程回归正常（关联 REQ-20260915-003）。 |
| BUG-20260916-001 | 2026-09-16 | P2 | 批量导入取消选择后页面残留「选择文件/未选择文件」原生控件 | 已关闭 | 任意页面底部视觉残留（手机/电脑端） | `js/ui.js` `pickFiles()`；批量导入流程 | 点「批量导入」→「选择文件夹开始」→ 取消文件对话框 → 页面底部出现「选择文件/未选择文件」控件，重复操作累积多个 | 根因：`pickFiles()` 动态 input append 到 body 未隐藏，仅选中文件（onchange）才 remove，取消对话框时残留且浏览器默认渲染原生控件。修复：`input.style.display='none'` + 监听 `cancel` 事件及时 remove。验证：`node --check` 通过；本地 8123 实测所有 file input 均 hidden，不再显示原生控件（详见 BUG-20260916-001.md）。 |
| BUG-20260916-002 | 2026-09-16 | P2 | 学习计划分组标题「今天」重复显示（今天 ·今天） | 已关闭 | 学习计划页分组标题（仅显示，无数据影响） | `js/views/plan.js` 分组标题；学习计划列表 | 截止日期为今天的任务分组标题显示「今天 ·今天」，看起来有两个「今天」 | 根因：`dateLabel()` 对今天的日期已返回「今天」，分组标题又追加了一次「·今天」（`k === todayStr()` 分支），重复拼接。修复：分组标题直接取 `dateLabel(k)`，删除冗余追加。验证：`node --check` 通过；标签逻辑单测 今天/昨天/明天/普通日期 均只显示一次（详见 BUG-20260916-002.md）。 |
| BUG-20260916-003 | 2026-09-16 | P2 | 学习时长只能增不能减：误录后无法清零/纠正 | 已关闭 | 首页学习时长记录与统计 | `js/controller.js` `logStudy`；`js/views/dashboard.js` 保存增量 | 误录 30 分钟后把输入改为 0，保存后仍是 30 | 根因：保存提交「新值−旧值」增量，负增量被 `logStudy` 的 `minutes > 0` 守卫直接丢弃。修复：`logStudy` 接受负增量并对结果 `Math.max(0, …)` 钳制，0 与 NaN 仍拒绝。验证：`node --check` 通过；边界用例 6 项（清零/减小/增加/0 不落库/越减钳 0/NaN 拒绝）全过（详见 BUG-20260916-003.md）。 |
| BUG-20260916-004 | 2026-09-16 | P3 | 模考成绩/真题管理表单缺输入校验（负数/0 分/分数>满分可保存） | 已关闭 | 模考成绩、真题管理两个表单 | `js/views/mockExam.js`、`js/views/pastPaper.js` 保存逻辑 | 分数填 -5 或 150（满分 100）可正常保存 | 根因：仅校验空值，`min=0` 只是浏览器提示非强制；真题空用时被 `Number('') \|\| 0` 静默转 0。修复：保存前校验数字合法性（≥0、满分>0、分数≤满分、NaN 拒绝）；真题空用时改存 null。验证：`node --check` 通过（详见 BUG-20260916-004.md）。 |