# Bug 台账

## 状态约定

- 待确认 / 复现中 / 修复中 / 待验证 / 已关闭 / 无法复现 / 不修复（保留原因）

| ID | 日期 | 严重度 | 标题 | 状态 | 影响范围 | 关联流程/测试 | 可复现或可验证的用户行为 | 摘要/备注 |
|----|------|--------|------|------|----------|---------------|--------------------------|----------|
| BUG-20260915-001 | 2026-09-15 | P1 | 导出备份时图片数据丢失（Blob JSON 序列化后为空对象 `{}`） | 已关闭 | 所有含图片的记录（错题簿图片、资料库图片）导出→导入恢复失效 | localStorage/IndexedDB 图片链路；导出/导入 | 用户导出备份，JSON 中 `images` 数组的图片 value 为 `{}`；导入后图片丢失 | 根因：`Blob` 不能直接 `JSON.stringify`，序列化后变空对象。修复：导出时 FileReader 转 base64 dataURL，导入时 atob 还原 Blob 写 IndexedDB；兼容旧版空对象备份（跳过+提示）。已改 `js/controller.js`。验证：Node 字节级往返测试（257B PNG dataURL 还原 size/字节一致），浏览器控制台确认图片入 IndexedDB 正常、页面无报错。 |
| BUG-20260915-002 | 2026-09-15 | P2 | 复习弹窗右上角 ✕ 关闭按钮失效（点 X 无反应，被强制二选一） | 已关闭 | 错题簿复习弹窗 | `wrongBook.js` 复习弹窗；复习流程 | 打开复习弹窗后点 ✕ 无反应，只能点「做对了/又做错了」退出 | 根因：`openReview` 用 `modalShell` 渲染带 ✕ 标题栏但漏调 `bindModalEvents()`，✕ 未绑定关闭事件（其余 6 视图弹窗均调用）。修复：补调 `bindModalEvents()`。验证：浏览器实测 ✕ 点击后弹窗关闭、做对流程回归正常（关联 REQ-20260915-003）。 |
| BUG-20260916-001 | 2026-09-16 | P2 | 批量导入取消选择后页面残留「选择文件/未选择文件」原生控件 | 已关闭 | 任意页面底部视觉残留（手机/电脑端） | `js/ui.js` `pickFiles()`；批量导入流程 | 点「批量导入」→「选择文件夹开始」→ 取消文件对话框 → 页面底部出现「选择文件/未选择文件」控件，重复操作累积多个 | 根因：`pickFiles()` 动态 input append 到 body 未隐藏，仅选中文件（onchange）才 remove，取消对话框时残留且浏览器默认渲染原生控件。修复：`input.style.display='none'` + 监听 `cancel` 事件及时 remove。验证：`node --check` 通过；本地 8123 实测所有 file input 均 hidden，不再显示原生控件（详见 BUG-20260916-001.md）。 |
| BUG-20260916-002 | 2026-09-16 | P2 | 学习计划分组标题「今天」重复显示（今天 ·今天） | 已关闭 | 学习计划页分组标题（仅显示，无数据影响） | `js/views/plan.js` 分组标题；学习计划列表 | 截止日期为今天的任务分组标题显示「今天 ·今天」，看起来有两个「今天」 | 根因：`dateLabel()` 对今天的日期已返回「今天」，分组标题又追加了一次「·今天」（`k === todayStr()` 分支），重复拼接。修复：分组标题直接取 `dateLabel(k)`，删除冗余追加。验证：`node --check` 通过；标签逻辑单测 今天/昨天/明天/普通日期 均只显示一次（详见 BUG-20260916-002.md）。 |
| BUG-20260916-003 | 2026-09-16 | P2 | 学习时长只能增不能减：误录后无法清零/纠正 | 已关闭 | 首页学习时长记录与统计 | `js/controller.js` `logStudy`；`js/views/dashboard.js` 保存增量 | 误录 30 分钟后把输入改为 0，保存后仍是 30 | 根因：保存提交「新值−旧值」增量，负增量被 `logStudy` 的 `minutes > 0` 守卫直接丢弃。修复：`logStudy` 接受负增量并对结果 `Math.max(0, …)` 钳制，0 与 NaN 仍拒绝。验证：`node --check` 通过；边界用例 6 项（清零/减小/增加/0 不落库/越减钳 0/NaN 拒绝）全过（详见 BUG-20260916-003.md）。 |
| BUG-20260916-004 | 2026-09-16 | P3 | 模考成绩/真题管理表单缺输入校验（负数/0 分/分数>满分可保存） | 已关闭 | 模考成绩、真题管理两个表单 | `js/views/mockExam.js`、`js/views/pastPaper.js` 保存逻辑 | 分数填 -5 或 150（满分 100）可正常保存 | 根因：仅校验空值，`min=0` 只是浏览器提示非强制；真题空用时被 `Number('') \|\| 0` 静默转 0。修复：保存前校验数字合法性（≥0、满分>0、分数≤满分、NaN 拒绝）；真题空用时改存 null。验证：`node --check` 通过（详见 BUG-20260916-004.md）。 |
| BUG-20260921-001 | 2026-09-21 | P1 | 手机端导出备份卡死在「正在生成备份文件…」或跳「无法访问此网站」错误页 | 已关闭 | 手机端导出备份（图片总量大时电脑端同样有风险） | `js/controller.js` `exportAll()`；导出/导入链路 | 手机点「导出数据」：进度条卡住不动，或浏览器跳到 `blob:` 链接的「无法访问此网站」页面 | 根因：① 导出把全部图片转 base64 后整体 `JSON.stringify` 成一根超大字符串、再复制成 Blob，峰值内存约为图片总量的 4~5 倍，手机标签页内存额度不足→页面冻结或被系统杀掉（stringify 同步执行，期间进度条冻结）；② 下载靠 blob: 临时链接+模拟点击触发，Android 浏览器对超大 blob 的下载常失败并降级为「打开该链接」，且代码 2 秒即 `revokeObjectURL`，下载管理器尚未开始读取链接即失效→「无法访问此网站」。修复：JSON 改为分段拼装 parts 数组、逐张转换后即释放 Blob 引用、Blob 组装后立即清空 parts，峰值内存约降一半以上；下载 `<a>` 先挂载 DOM 再点击后移除；revoke 延长到 60 秒。备份文件格式与内容与旧版逐字节等价，导入侧无需改动。验证：`node --check` 通过；分段拼装 vs 整体 stringify 等价性单测 8/8（`.tools/test-export-json.js`，覆盖 0/1/多张、脏数据跳过、特殊字符）；浏览器实测（localhost:8123）注入 2 张测试图+存量 13 张图片导出成功，备份 6486 字节、`backup/createdAt/data/images` 结构完整、缩略图键正确跳过、下载文件名 `backup-2026-09-21.json`、控制台无报错。TECH/PRD 无需更新：备份格式、文件命名与用户流程均未变化。 |
| BUG-20260922-001 | 2026-09-26 | P2 | `.gitignore` 未覆盖 v2 备份扩展名 `.jsonl`，124.8MB 备份处于未跟踪状态 | 已关闭 | 仓库卫生；页面发布流程（`git add .` 误提交后 `git push` 会被 GitHub 100MiB 单文件上限拒绝） | `.gitignore`；`git status` | `git status` 显示 `backup-2026-09-22.jsonl`（124.80 MiB）为 untracked，处于「可被 `git add .` 收进来」的状态 | 根因：REQ-20260921-001 把导出文件名从 `backup-*.json` 改为 `backup-*.jsonl`，但 `.gitignore` 只忽略了 `backup-*.json`。修复：`.gitignore` 增加 `backup-*.jsonl`。验证：`git check-ignore -v backup-2026-09-22.jsonl` 命中 `.gitignore:12`，`git status` 不再列出该文件。 |
| BUG-20260922-002 | 2026-09-26 | P1 | 导入前不做配额预检，空间不足时跑到一半才失败 | 已关闭 | 导入备份（万题量级备份约 12.2GB） | `js/controller.js` `importAll()`；导入链路 | 剩余磁盘空间小于备份体积时点导入，进度条走到中途才报错，白等数十分钟 | 根因：全代码无一处在导入前调用 `navigator.storage.estimate()`（仅设置弹窗用于展示）。修复：`importAll()` 在任何写入前预检 `quota - usage`，需求量按文件体积 × 0.8 估算；不足则弹「存储空间可能不足」确认框（取消=不写入任何数据 / 仍要导入=继续），预检自身异常不拦截（有回滚兜底）。验证：e2e S4（取消后 `ic1` 未写入、文案含「约需 205 B，本机仅剩约 50 B」）、S5（点「仍要导入」后导入成功）均通过。 |
| BUG-20260922-003 | 2026-09-26 | P1 | 导入非原子：中途失败留下半截图片数据且无法续传 | 已关闭 | 导入备份（万题量级下重来需 18–45 分钟） | `js/controller.js` `importAll()`；导入链路 | 导入一份较大备份时中途失败（文件损坏/空间不足/页面被杀），再次导入前 IndexedDB 里已混入部分「孤儿」图片 | 根因：图片逐张 `saveImage` 落库，`state` 最后才 `replace()`，且全程只有单一 `catch`，失败后已写入的图片无人清理。修复：导入前记录 `preExisting = Set(imageKeys())` 快照；每张图落库即登记进 `written`；失败时删除 `written - preExisting`（原图与 `t_` 缩略图键一并删），并单独提示「正在撤销已导入的内容…」；**导入前已存在的同 ID 图不删**，重导同一份备份不误伤旧数据。验证：e2e S2（ib1/ib2 被回滚、ia1/ia2 保留、错题数不变）、S7（iv1 已存在→保留，inew1 新写入→回滚）均通过，刷新后无残留。 |
| BUG-20260922-004 | 2026-09-26 | P3 | 导入失败文案误导：所有异常统一报「文件格式不正确」 | 已关闭 | 导入备份的失败反馈 | `js/controller.js` `importAll()`；导入链路 | 因空间不足或磁盘被占满导致导入失败时，提示「导入失败：文件格式不正确」，用户会误以为备份文件坏了而反复重导 | 根因：`catch` 内硬编码统一文案，未按错误类型分流。修复：新增 `importFail()`（标记用户可读错误）与 `importErrMsg()`，按 `userMsg` / `QuotaExceededError` / 其他分流为「这不是有效的备份文件（首行不是备份数据）」「备份文件第 N 行数据损坏」「这不是本应用的备份文件（缺少备份数据）」「备份中有一张图片数据损坏，无法解码」「导入中途存储空间不足」，并在有内容被撤销时追加「已自动撤销本次导入的内容」、清理失败时追加「（N 张图片未能清理，不影响现有数据）」。验证：e2e S2/S3/S7 的 toast 文案逐条比对通过。 |