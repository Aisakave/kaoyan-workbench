# PROG-REQ-20260921-001 大容量流式备份

关联需求：`Requirements/REQ-20260921-001.md`

## 阶段计划

| # | 阶段 | 内容 | 状态 |
|---|---|---|---|
| 1 | 实现-导出 | `exportAll()` FSA 流式 NDJSON 写盘 + 降级路径 | 已完成 |
| 2 | 实现-导入 | `importAll()` 流式逐行解析 + v1 兼容 + 降级 | 已完成 |
| 3 | 测试 | NDJSON 等价/解析单测；浏览器 e2e（导出/v2 导入/v1 导入/字节比对） | 已完成 |
| 4 | 文档 | TECH §8 更新、需求/进度台账回填 | 已完成 |
| 5 | 用户真机验收 | 用户在电脑 Chrome/Edge 实际导出/导入验收 | 待验证 |

## 进度记录

### 2026-09-22（发布）

- 已提交并推送 main（e45ab5d），GitHub Pages 构建完成，线上 `js/store.js` 经 CDN 拉取确认为新版（含 v2 流式备份逻辑）。
- 用户线上验收路径：打开线上地址（必要时 Ctrl+F5 清缓存）→ 侧栏「导出数据」→ 进度条平滑走完 → 得到 `backup-YYYY-MM-DD.jsonl`。

### 2026-09-21（实现与验证完成）

- 需求与方案经用户确认（方案 A / Chrome-Edge / 不做记住位置与压缩），见 REQ 文档「用户确认口径」。
- 实现：`store.js` 新增 `imageKeys()`；`controller.js` 重构为 `exportAll()`（分流）→ `exportStream()`（v2 流式）/ `exportBlob()`（v1 降级），`importAll(file)` 流式逐行解析自动识别 v2/v1；`app.js` 导入改传 File；`index.html` 导入选择器接受 `.jsonl`。
- 单测：`.tools/test-export-ndjson.js` 10/10 通过（1 字节极限 chunk 切断、chunk 1~7 全采样、末行无换行、v1 识别、空行/未知行、多字节 UTF-8、1MB 大行往返）；`node --check` 全过（controller/app/store）。
- 浏览器 e2e（localhost:8124，mock showSaveFilePicker + 注入 1x1 PNG×2 + 缩略图×1）：
  - 导出：3 次流式写入、流正常 close；文件名 `backup-2026-09-21.jsonl`；meta 结构完整（v:2/createdAt/data.settings/imagesTotal:2）；缩略图 `t_` 键正确跳过；**导出图片与库内原图逐字节一致（byteIdentical=true，清晰度零损失）**
  - 导入 v2：删图后从 NDJSON File 流式恢复，两图字节还原（size 一致）
  - 导入 v1：旧格式单根 JSON File 恢复图片 + state 替换正确（examDate=2027-12-18）
  - 本次流程控制台零新增报错
- 测试过程中的环境问题（非应用缺陷，已排查确认）：自动化测试的坐标点击未触发按钮 onclick（用户真实点击不受影响）；测试 evaluate 工具对 `const` 顶层声明与 `\n` 转义的限制。

## DoD

- REQ 验收标准 6 项：1~6 全部通过并留证据（见上）；用户真机验收（阶段 5）待进行。

## 风险与依赖

- `showSaveFilePicker` 需在用户手势激活窗口内调用：实现为点击导出按钮后先取文件句柄再弹进度窗（`exportAll` 同步分流，无中间 await），已规避激活过期。
- 用户取消「另存为」抛 AbortError：静默退出，不算失败（已实现）。
- Chromium 流式写大文件稳定性依赖浏览器实现：单测与 mock e2e 已覆盖逻辑正确性；数 GB 真实量级待用户真机验收（当前 100 题 123MB 量级风险低）。
