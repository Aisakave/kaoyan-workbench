# 进度 PROG-REQ-20260915-013 错题本批量导入（本地图片文件夹）

> 关联需求：`Requirements/REQ-20260915-013.md`

## 阶段记录

| 阶段 | 内容 | 状态 | 证据/说明 |
|---|---|---|---|
| S1 调研 | 确认磁盘图片无法绕过应用写入浏览器存储；现有「导入备份」为整体覆盖不可复用；`pickImages` 不支持选文件夹 | 完成 | store.js/controller.js/ui.js 代码核对；习题1 实图 4 张（1671x1224 / 911x215 / 858x282 / 2319x1250） |
| S2 口径确认 | 配对=题目+答案合 1 条 2 图；单数尾图单独成条；预览确认；默认科目数学、标题「习题N」、错因选其他、关键一步留空；只新增不覆盖；created 取题目图文件时间 | 完成 | 用户两次确认（AskUserQuestion + REQ-013 审查） |
| S3 实现 | ui.js 加 `pickFiles`（原始 File 数组 + webkitdirectory）；wrongBook.js 加批量导入按钮+全流程（配对/预览/移除/写入/进度）；components.css 加 `.bi-*` 预览样式 | 完成 | node --check 2 文件通过；修复 select 缺 selected 导致默认科目变英语的 bug |
| S4 验证 | node 配对逻辑 + Edge 真实浏览器实测 | 完成 | 全链路 9 项 ✅（见 REQ-013 第五节）：单图/4图2对配对、created 顺序还原、默认科目数学、移除条目、IndexedDB 落库、console 零报错；待用户真实文件夹实测确认「习题N」标题 |

## 风险与注意

- 只新增错题记录，不触碰既有数据与其它视图；无数据迁移（无新增字段）。
- 预览阶段不写 IndexedDB，确认后才落库；单张 >12MB 或非图片文件跳过并提示。
- 手机端不支持 `webkitdirectory`，降级为多选图片，配对逻辑不变。
- 导入图片走与手工上传完全一致的「原图 + 缩略图」链路，列表/原图/删除释放行为一致。
