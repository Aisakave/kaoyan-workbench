# 进度 PROG-REQ-20260915-012 错题卡片显示添加时间 + 排序切换

> 关联需求：`Requirements/REQ-20260915-012.md`

## 阶段记录

| 阶段 | 内容 | 状态 | 证据/说明 |
|---|---|---|---|
| S1 调研 | 确认数据本就存 `created/updated`，仅缺展示 | 完成 | controller.js `addWrongQuestion` 已写 `created/updated`；列表已按 updated 排序 |
| S2 口径确认 | 选定「卡片显示时间 + 排序切换」 | 完成 | AskUserQuestion 明确选择（REQ-012 第二节） |
| S3 实现 | utils.js 加 `relTime`；wrongBook.js 加 `sortBy/sortDir/sortWrong`+字段切换条+方向切换+卡片时间；components.css 加 `.wc-time` | 完成 | node --check 通过 |
| S4 验证 | 本地服务 + 无头浏览器播种 3 条真实数据实测 | 完成 | 卡片时间文案✅、字段切换条✅；方向「最早在前」倒序✅（最早A/中间B/最新C）、文案与箭头就地更新✅、往返稳定✅、console 零报错✅ |

## 风险与注意

- 本次改动未触碰其它视图（资料库/首页/统计），也未改动数据存储结构，无迁移。
- 排序切换仅触发结果区刷新，沿用「分离更新」模式，不影响输入框焦点。
- 历史旧数据缺 `created` 时回退 `updated`，显示与排序均兼容。