# 考研备考工作台 · 功能进度（错题复习与排行榜）

> 关联需求：`Requirements/REQ-20260915-002.md`
> 状态：已归档
> 最后更新：2026-09-15

## 目标、范围与确认口径

- 已确认目标：为错题簿新增「错题排行榜 + 错题复习」：按复发次数排行 Top 10（前三奖牌）；间隔自适应递增（3/5/7/10/14/21/30 天）提醒复习；复习 = 重做可标对错（做对清零递增间隔 / 做错再错 +1 并回 3 天重学）；侧栏 + 手机 Tab 红点徽标提醒。
- 包含：数据迁移（`reviewCount` / `lastReviewedAt`，`_version` 1→2）、controller 三个新方法、错题簿三标签（待复习/全部/排行榜）、复习对错弹窗、侧栏/Tab 徽标、逾期分级样式。
- 不包含：复习历史轨迹（本期只记最近一次）、首页待复习卡、自定义间隔设置、系统通知推送。
- 关联决策：方案 A（错题簿内三标签）已确认 2026-09-15；间隔自适应递增已确认。
- 主要风险与依赖：`wrongBook.js` 渲染改动较大，需保证搜索输入焦点不丢失（沿用分离更新模式）；红点徽标需在数据变更后及时刷新；纯前端无自动化测试环境，需真实浏览器实测。

## 阶段计划

| 阶段 | 目标与交付物 | 前置条件 | DoD / 验证证据 | 状态 |
|---|---|---|---|---|
| R1 数据与逻辑层 | `store.js` 迁移 v2 + `controller.markReviewed / reviewDueList / ranking` | 方案定稿 | `node --check` 通过；迁移与到期计算逻辑 Node 脚本验证通过 | ✅ 完成 |
| R2 错题簿三标签 | `wrongBook.js`：待复习/全部/排行榜 + 复习对错弹窗 + 逾期标记 | R1 | 三标签切换正确；复习后时间清零/再错+1/间隔递增；搜索焦点不丢失 | ✅ 完成 |
| R3 徽标与样式 | `router.js` 侧栏/Tab 红点徽标 + `css` 逾期分级/排行榜样式 | R1 | 红点数字 = 到期数；点击直达「待复习」标签；逾期黄/橙/红正确 | ✅ 完成 |
| R4 联调验收 | 浏览器实测全链路 + 文档回填（REQ 验收矩阵、README） | R1~R3 | REQ-20260915-002 行为验收矩阵逐项通过 | ✅ 完成 |

## 进度日志

### 2026-09-15 — 需求与方案确认（P0）

- 完成：需求澄清（排行口径 / 复习语义 / 提醒方式 / 间隔规则四项决策确认）；整体方案 A 确认（间隔天数和排行榜条数用户确认无需调整）；本文档与 `REQ-20260915-002` 创建；台账更新。
- 验证证据：`Requirements/REQ-20260915-002.md` 行为验收矩阵。
- 阻塞或变更：无。
- 下一步：开始 R1 数据与逻辑层。

### 2026-09-15 — R1 数据与逻辑层（P1）

- 完成：`utils.js` 新增 `REVIEW_INTERVALS` / `nextReviewInterval` / `reviewDue` / `reviewMeta` / `overdueLevel` 纯函数；`store.js` `_version` 1→2，`migrate()` 逐条补 `reviewCount`/`lastReviewedAt`；`controller.js` 新增 `markReviewed` / `reviewDueList` / `reviewDueCount` / `ranking`，`addWrongQuestion` 补默认字段。
- 验证证据：`node --check` 5 个改动 JS 全部通过；Node 断言 13 项全 PASS（迁移补齐、从未复习=创建+3 天、做对递增、做错归零+≥3 永久保留、排行榜降序与次级排序、间隔首项 3 与封顶 30）。
- 阻塞或变更：断言脚本两处修正均为测试数据问题（replace 不触发迁移需显式 `Store.migrate()`；同 `recurCount` 次级排序测试数据的 updated 需与期望一致），非实现缺陷。
- 下一步：R2 错题簿三标签。

### 2026-09-15 — R2/R3 视图与徽标（P2）

- 完成：`wrongBook.js` 重构为三标签结构（`render` 分派 + `renderAllTab`/`renderReviewTab`/`renderRankTab`），复习对错弹窗 `openReview`/`finishReview`，全部标签卡片增加复习按钮与复习状态文案；`router.js` 增加 `parseHash` 查询参数剥离、侧栏/Tab 红点徽标 `refreshBadges`、徽标点击直达 `#/wrong?tab=review`、`change` 事件联动刷新；`css/components.css` 与 `css/layout.css` 追加标签/逾期分级/排行榜/手机 Tab 徽标样式。
- 验证证据：`node --check` 通过；浏览器实测三标签切换、复习弹窗、逾期分级样式均正常。
- 阻塞或变更：无。
- 下一步：R4 联调验收与文档回填。

### 2026-09-15 — R4 联调验收与文档回填（P3）

- 完成：真实浏览器全链路实测（注入 `_version=1` 旧格式种子数据 6 题，桌面 + 375px 手机宽度）：
  - 数据迁移：内存态立即补齐字段；首次数据变更后物理落盘 `_version=2` 且字段完整（惰性迁移闭环确认）。
  - 待复习：到期队列 5 题（含永久保留题），排序与逾期分级（红/橙/黄）正确，徽标数字 5↔3 随复习动态变化。
  - 复习流程：做对 →「下次 5 天后到期」+ 移出队列；做错 → `recurCount+1`、`reviewCount` 归零 + Toast；reload 后记录保留、徽标正确。
  - 排行榜：`recurCount` 降序，同次数按最近更新降序，前三 🥇🥈🥉。
  - 全部标签搜索连续输入焦点不丢失；手机端底部 Tab 徽标显示、无横向溢出。
- 验证证据：`Requirements/REQ-20260915-002.md` 验收矩阵 10 项全部 ✅；REQ/PROG/台账/README 同步回填。
- 阻塞或变更：一次中间断言误报（读取时机问题）经最小化复核确认为非 Bug；实测记录行为细节（惰性迁移、永久保留题进队列）已写入 REQ。
- 下一步：无（需求完成）。

## 最终结果

- 全部完成（2026-09-15）：错题簿三标签（待复习/全部/排行榜）、复习对错闭环（间隔 3→5→7→10→14→21→30 天自适应递增、做错回 3 天重学并 ≥3 永久保留）、侧栏/手机 Tab 红点徽标直达待复习、逾期黄/橙/红分级、排行榜 Top10 前三奖牌。
- 验证：Node 断言 13 项全 PASS + 浏览器实测桌面/手机全链路通过，REQ 验收矩阵 10 项全 ✅。
- 遗留项：无（复习历史轨迹、首页待复习卡、自定义间隔设置、系统推送按范围确认不包含）。
