# 进度 PROG-REQ-20260915-014 批量导入增强（科目识别/日期标题/错因可改/一键全改科目）

> 关联需求：`Requirements/REQ-20260915-014.md` · 前置：REQ-013（已验证）

## 阶段记录

| 阶段 | 内容 | 状态 | 证据/说明 |
|---|---|---|---|
| S1 口径 | 用户确认：标题=整个文件夹名；错因用下拉单选；一键全改；科目/日期识别规则 | 完成 | AskUserQuestion 两项确认 + REQ-014 落档 |
| S2 实现 | parseFolderMeta + 预览弹窗（识别提示/全改按钮组/错因下拉） | 完成 | node --check 通过；9 项规则单测 ALL PASS |
| S3 验证 | 浏览器实测：未识别提示、一键全改（2 条全变英语）、错因改 calc、落库 subject/errorTypes/created 全对、console 零报错 | 完成 | 模拟文件无 webkitRelativePath，「已识别」绿色提示路径由规则单测覆盖，待用户真实文件夹实测 |
| S4 首次引导 | 「批量导入」首次弹引导（4 步流程 + 配对规则 + 勾选跳过），`kylc:bulkGuide` 控制，两条路径浏览器实测通过 | 完成 | 过程中修复误用 Store.get 的 bug（Store 无 get/set，改 localStorage 原生读写）；localhost 旧缓存导致测试一度加载旧代码，换 127.0.0.1 新域验证 |

## 风险与注意

- 仅改批量导入预览弹窗与写入参数，不动数据 schema；不触碰其它视图。
- 文件夹名识别是「引导非强制」：识别不到仅提示，不拦截导入。
- 错因下拉为单选；多错因场景仍走导入后编辑。
